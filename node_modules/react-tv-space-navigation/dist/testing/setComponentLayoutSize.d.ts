import { RenderResult } from '@testing-library/react-native';
export declare const setComponentLayoutSize: (listTestId: string, component: RenderResult, size: {
    width: number;
    height: number;
}) => void;
