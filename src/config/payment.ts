// ============================================================================
// PAYMENT CONFIG
// ค่าตั้งค่าฝั่งร้านค้า แก้ตรงนี้ที่เดียว ห้าม hardcode เลขบัญชี/พร้อมเพย์
// กระจายอยู่ในไฟล์อื่น
// ============================================================================

/**
 * เบอร์พร้อมเพย์ของร้าน — รับได้ 2 แบบ:
 *  - เบอร์มือถือ 10 หลัก ขึ้นต้นด้วย 0 (เช่น "0812345678")
 *  - เลขบัตรประชาชน/เลขผู้เสียภาษีนิติบุคคล 13 หลัก
 *
 * ⚠️ TODO: เปลี่ยนเป็นเบอร์/เลขจริงของร้านก่อนขึ้นโปรดักชัน ค่าด้านล่างเป็น
 * เบอร์ตัวอย่างเท่านั้น สแกนแล้วเงินจะไม่เข้าบัญชีร้านจริงถ้าไม่เปลี่ยน
 */
export const PROMPTPAY_ID = '0812345678';
