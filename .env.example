# คัดลอกไฟล์นี้เป็น .env แล้วใส่ค่าจริง
# Expo จะอ่านตัวแปรที่ขึ้นต้นด้วย EXPO_PUBLIC_ เข้าไปใช้ในแอปได้โดยตรง

# URL ของ backend (main-service/apps/backend ที่มีอยู่แล้ว)
# ตอน dev บนเครื่องจริง/emulator ให้ใช้ IP เครื่อง ไม่ใช่ localhost
EXPO_PUBLIC_API_URL=http://192.168.1.100:3000/api
